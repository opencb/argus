# CUSTOM VALIDATOR
validator: /home/dapregi/dev/argus/opencga_validator.py

# WORKING DIR FOR CUSTOM INPUT/OUTPUT FILES
workingDir: /home/dapregi/tmp/argus

# BASE URL
baseUrl: https://test.app.zettagenomics.com/xeta-14s/opencga/webservices/rest/v2

# AUTHENTICATION INFO
#authentication:
#  # token: ssasasdsadsadasd.sdasd.asdasd
#  # token: env(OPENCGA_TOKEN)
#  token: login(responses[0].results[0].token)
#  url: https://test.app.zettagenomics.com/xeta-19e/opencga/webservices/rest/v2/users/login
#  headers:
#    Accept-Encoding: gzip
#  method: POST
#  body: {"user": "USER", "password": "PASS"}

# DEFAULT REST PARAMETERS
#rest:
#  headers:
#    Accept-Encoding: gzip
#  queryParams:
#    limit: 10
#    count: False

# SELECTED SUITES FOR TESTING
suites:
  - gene

# TEST SELECTION
#tests:
#  ignore_async: false
#  ignore_method: ["POST"]
#  ignore_tag: ["SLOW"]

# VALIDATION PARAMETERS
validation:

  timeDeviation: 5  # +/- seconds allowed to deviate from expected time
  asyncRetryTime: 10  # retry time for checking asynchronous jobs

  ignoreTime: false  # ignore validation of response time
  ignoreHeaders: []  # ignore validation of header parameters (e.g. ["Content-Type"])
  ignoreResults: []  # ignore validation of particular methods (e.g. ["match", "list_contains"])

  failOnFirst: False  # stop validation in first FAIL
