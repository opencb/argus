id: cohort
tests:
  - id: create
    path: /cohorts/create
    method: POST
    async: false
    steps:
      - id: step1
        queryParams:
          study: 1000G
          includeResult: true
        bodyParams:
          id: cohort4
        validation:
          results:
            - match('responses[0].results[0].id', 'cohort4')
