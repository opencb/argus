id: cohort
tests:
  - id: create
    path: /cohorts/create
    method: POST
    async: false
    tasks:
      - id: task1
        queryParams:
          study: 1000G
          includeResult: true
        body:
          id: cohort2
        validation:
          results:
            - match('responses[0].results[0].id', 'cohort2')
